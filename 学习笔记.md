# Vibe Coding 课程设计学习笔记

## 一、课程设计概述

本课程设计以"系统学习 Vibe Coding（AI 编程）方法与工具，并完成面向制造场景的功能完善应用程序开发"为目标。Vibe Coding 的核心思想是：开发者以自然语言描述意图，由 AI 大模型驱动代码生成、调试、重构与文档编写，人类角色从"逐行编码者"转变为"需求定义者 + 架构决策者 + 结果审查者"。

作为智能制造工程专业学生，本次设计将 AI 编程能力与制造场景需求结合，最终交付一个可运行、可维护、有实际工程价值的应用程序。

---

## 二、AI 编程工具学习总结

### 2.1 Vibe Coding 核心方法论

Vibe Coding 并非简单的"让 AI 写代码"，而是一套完整的开发范式，包含以下关键环节：

| 环节 | 说明 |
|------|------|
| **意图表达** | 用清晰的自然语言描述功能需求、输入输出、约束条件 |
| **迭代生成** | AI 生成初版代码 → 人工审查 → 提出修改意见 → 再次生成 |
| **上下文管理** | 维护项目级上下文（技术栈、架构、编码规范），保证 AI 输出一致性 |
| **人工兜底** | 对关键逻辑、安全边界、性能瓶颈进行人工审查与测试 |
| **知识沉淀** | 将每次交互中的有效 Prompt、解决方案沉淀为项目文档 |

### 2.2 主流 AI 编程工具对比

#### （1）代码补全与对话类

- **GitHub Copilot**：基于 OpenAI Codex，深度集成 VS Code / JetBrains，擅长行级补全、函数生成、注释转代码。优点是生态成熟、响应快；缺点是项目级理解有限，复杂架构设计能力弱。
- **Cursor**：以 AI 为核心的编辑器，支持 `Cmd+K` 行内编辑、`Cmd+L` 对话、Composer 多文件编辑。对整个代码库的上下文理解能力强，适合 Vibe Coding 全流程。
- **Codeium / Windsurf**：免费替代方案，Windsurf 的 Cascade 功能支持多步骤自主编码任务。
- **通义灵码 / 豆包 MarsCode / 文心快码**：国产工具，中文理解好，对国内开发者友好，部分支持私有化部署。

#### （2）Agent 自主编程类

- **Claude Code**：Anthropic 官方 CLI 工具，支持在终端中自主读写文件、执行命令、调试错误，适合复杂任务的端到端实现。
- **OpenAI Codex CLI**：OpenAI 推出的命令行编码 Agent，可自主执行多步开发任务。
- **Aider**：开源终端 AI 编程工具，支持 Git 自动提交、多模型切换、代码地图（repo map），对 Git 工作流友好。
- **Devin / Cognition**：全自主软件工程 Agent，能独立完成从需求理解到部署的全流程，但成本较高。

#### （3）通用大模型辅助

- **ChatGPT（GPT-4o / o1 / o3）**：适合算法设计、代码解释、Bug 分析、技术方案对比。
- **Claude（Sonnet / Opus）**：长上下文能力强，适合阅读大型代码库、生成完整模块。
- **DeepSeek-Coder / Qwen-Coder**：开源代码大模型，可本地部署，数据敏感场景适用。

### 2.3 Prompt 工程实践要点

在 Vibe Coding 中，Prompt 质量直接决定代码质量。实践中总结出以下有效模式：

1. **角色设定**：明确 AI 扮演的角色，如"你是一名资深 Python 后端工程师，熟悉 FastAPI 和工业通信协议"。
2. **上下文注入**：提供项目技术栈、目录结构、已有代码片段、编码规范。
3. **任务拆解**：将大需求拆为小步骤，每步验证后再继续，避免一次性生成数百行不可控代码。
4. **约束明确**：指定输入输出格式、错误处理方式、性能要求、禁止使用的库。
5. **示例驱动**：给出输入输出示例，比纯文字描述更精准。
6. **迭代追问**：对生成结果不满足时，明确指出"哪里不对 + 期望怎样"，而非重新描述整个需求。

### 2.4 AI 编程的局限性与应对

| 局限性 | 表现 | 应对策略 |
|--------|------|----------|
| **幻觉** | 编造不存在的 API、库函数、参数 | 查阅官方文档验证，运行测试 |
| **上下文丢失** | 长对话中遗忘早期约束 | 定期重置上下文，在 Prompt 中重申关键约束 |
| **安全漏洞** | 生成含 SQL 注入、硬编码密钥的代码 | 人工安全审查，使用 SAST 工具扫描 |
| **过度工程** | 引入不必要的抽象和依赖 | 明确"最小可用"原则，控制依赖数量 |
| **领域知识不足** | 对制造领域特定协议/工艺理解浅 | 人工补充领域知识，提供参考文档 |

---

## 三、拟选用的 Harness 与模型选型

### 3.1 什么是 Harness

在 AI 编程语境下，**Harness（编程 harness / 编码脚手架）** 指承载 AI 模型、管理上下文、协调工具调用、执行代码并反馈结果的外层框架或工具链。它决定了 AI 能"看到什么"（上下文窗口）、"能做什么"（工具调用权限）、"如何迭代"（反馈循环）。

一个典型的 AI 编程 Harness 包含：

- **模型接入层**：对接 LLM API 或本地模型
- **上下文管理器**：维护对话历史、代码库摘要、文件内容
- **工具执行层**：文件读写、命令执行、Git 操作、浏览器访问
- **反馈循环**：执行代码 → 捕获输出/错误 → 传回模型 → 修正
- **安全沙箱**：限制 AI 的操作权限，防止误删/恶意操作

### 3.2 Harness 选型对比

| Harness | 类型 | 优势 | 劣势 | 适用场景 |
|---------|------|------|------|----------|
| **Cursor** | IDE 集成 | 上手快，多文件编辑强，UI 友好 | 重度依赖网络，定制化有限 | 日常开发、快速原型 |
| **Aider** | CLI 开源 | Git 友好，多模型支持，自动提交 | 纯终端操作，学习曲线稍陡 | 版本控制严格的项目 |
| **Claude Code** | CLI 官方 | 自主能力强，工具调用稳定 | 仅支持 Anthropic 模型，需订阅 | 复杂多步任务 |
| **Continue.dev** | IDE 插件开源 | 可自托管，支持本地模型，高度可定制 | 配置较复杂 | 数据敏感、私有化部署 |
| **OpenHands** | 开源 Agent | 全自主软件工程，沙箱安全 | 资源消耗大，稳定性待提升 | 研究探索、全自动化 |

### 3.3 本项目选型方案

**推荐组合：Cursor（主力）+ Aider（Git 工作流）+ 通用大模型（方案设计）**

- **日常编码主力：Cursor**
  - 理由：UI 友好、多文件同时编辑、项目级上下文理解好，适合快速迭代制造应用的前后端代码。
  - 模型：内置 Claude Sonnet 4 / GPT-4o，可按需切换。

- **Git 提交与重构：Aider**
  - 理由：自动生成规范的 Git commit message，支持 repo map 理解整个项目结构，适合阶段性重构和提交。
  - 模型：可配置 DeepSeek-Coder 或 Qwen2.5-Coder 降低成本。

- **方案设计与算法验证：ChatGPT / Claude 网页版**
  - 理由：长文本分析、架构对比、算法推导能力强，适合在编码前做技术选型和方案设计。

### 3.4 模型选型建议

| 用途 | 推荐模型 | 理由 |
|------|----------|------|
| **代码生成主力** | Claude Sonnet 4 / GPT-4o | 代码质量高，上下文理解好 |
| **复杂推理/算法** | GPT-o3 / Claude Opus | 逻辑推理强，适合算法设计 |
| **低成本批量任务** | DeepSeek-V3 / Qwen2.5-Coder-32B | 性价比高，中文支持好 |
| **本地/隐私场景** | Qwen2.5-Coder-7B / DeepSeek-Coder-V2 | 可本地部署，数据不出域 |

> 对于智能制造场景，若涉及工厂敏感数据，建议优先考虑可私有化部署的开源模型（如 Qwen2.5-Coder 系列），或使用支持企业级数据隔离的商业 API。

---

## 四、Git 原理与功能

### 4.1 Git 核心原理

Git 是一个**分布式版本控制系统**，其设计哲学基于以下核心概念：

#### （1）快照而非差异

与 SVN 等集中式系统存储"文件差异"不同，Git 每次提交时对全部文件制作**快照（Snapshot）**。未修改的文件通过哈希引用复用之前的对象，因此高效且完整。

#### （2）对象数据库

Git 内部维护四类对象，均通过 SHA-1 哈希唯一标识：

| 对象类型 | 说明 |
|----------|------|
| **Blob（二进制对象）** | 存储文件内容，不包含文件名 |
| **Tree（树对象）** | 存储目录结构，指向 blob 或子 tree，包含文件名和权限 |
| **Commit（提交对象）** | 指向一个 tree，包含作者、提交者、时间、消息、父提交指针 |
| **Tag（标签对象）** | 指向某个 commit，用于标记版本（如 v1.0.0） |

#### （3）三区模型

Git 的工作流程围绕三个区域展开：

```
工作区 (Working Directory)
    ↓ git add
暂存区 (Staging Area / Index)
    ↓ git commit
本地仓库 (Local Repository / .git)
    ↓ git push
远程仓库 (Remote Repository)
```

- **工作区**：实际编辑文件的地方
- **暂存区**：下次提交的内容清单，允许选择性提交
- **本地仓库**：完整的版本历史，存储在 `.git` 目录中

#### （4）分支即指针

Git 的分支本质上是一个**指向某个 commit 的可变指针**。创建分支成本极低（仅写一个 40 字节的文件），这使得 Git 的分支模型极其轻量高效。`HEAD` 指针指向当前所在分支。

#### （5）分布式架构

每个克隆都是完整的仓库副本，包含全部历史。即使远程服务器宕机，任意本地仓库都可作为完整备份。协作通过 `push` / `pull` / `fetch` 在仓库间同步。

### 4.2 Git 核心功能与常用命令

#### （1）基础操作

```bash
# 初始化仓库
git init
git clone <url>

# 查看状态
git status
git diff              # 工作区 vs 暂存区
git diff --staged     # 暂存区 vs 最新提交

# 提交
git add <file>        # 添加到暂存区
git add .             # 添加所有变更
git commit -m "message"
git commit -am "message"  # 跳过 add，直接提交已跟踪文件

# 查看历史
git log
git log --oneline --graph --all
git show <commit-hash>
```

#### （2）分支管理

```bash
git branch                # 列出分支
git branch <name>         # 创建分支
git checkout <name>       # 切换分支
git switch <name>         # 切换分支（新版推荐）
git switch -c <name>      # 创建并切换
git branch -d <name>      # 删除分支
git branch -D <name>      # 强制删除
```

#### （3）合并与变基

```bash
git merge <branch>        # 合并分支到当前分支
git rebase <branch>       # 变基：将当前分支的提交移到目标分支之后
git merge --abort         # 中止合并
git rebase --abort        # 中止变基
```

**Merge vs Rebase 选择原则**：

- `merge`：保留完整历史，产生合并提交，适合公共分支
- `rebase`：历史线性整洁，适合个人特性分支，但不要对已推送的公共分支使用

#### （4）远程协作

```bash
git remote -v                   # 查看远程仓库
git remote add origin <url>     # 添加远程仓库
git push -u origin main         # 推送并设置上游
git pull                        # 拉取并合并（= fetch + merge）
git fetch                       # 仅拉取，不合并
git push origin --delete <branch>  # 删除远程分支
```

#### （5）撤销与回退

```bash
git restore <file>              # 撤销工作区修改
git restore --staged <file>     # 从暂存区撤回
git reset --soft HEAD~1         # 回退提交，保留变更在暂存区
git reset --mixed HEAD~1        # 回退提交，保留变更在工作区（默认）
git reset --hard HEAD~1         # 回退提交，丢弃所有变更（危险）
git revert <commit>             # 生成新提交来撤销指定提交（安全）
```

#### （6）储藏与暂存

```bash
git stash               # 储藏当前工作
git stash list          # 查看储藏列表
git stash pop           # 恢复并删除储藏
git stash apply         # 恢复但保留储藏
```

### 4.3 AI 编程场景下的 Git 最佳实践

1. **频繁小步提交**：AI 生成代码后，每完成一个可验证的功能点就提交，便于回退。
2. **使用特性分支**：每个功能/实验开独立分支，`main` 保持稳定。
3. **AI 生成 Commit Message**：用 Aider 或 `git commit --amend` 让 AI 辅助生成规范的提交信息。
4. **提交前审查**：`git diff` 检查 AI 生成的代码，确认无误后再提交。
5. **保护敏感信息**：使用 `.gitignore` 排除 `.env`、密钥文件、模型缓存等。
6. **规范分支命名**：`feature/xxx`、`fix/xxx`、`docs/xxx`、`refactor/xxx`。
7. **Conventional Commits**：采用 `feat:`、`fix:`、`docs:`、`refactor:`、`test:` 等前缀，便于生成 Changelog。

### 4.4 .gitignore 模板（Python 项目）

```
# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp

# 环境变量
.env
.env.local

# 模型与数据
models/
*.pt
*.pth
data/raw/
*.csv
!data/sample/*.csv

# 日志
logs/
*.log

# 系统
.DS_Store
Thumbs.db
```

---

## 五、选题调研：面向制造场景的应用程序

### 5.1 智能制造场景需求分析

智能制造工程的核心是将信息技术、自动化技术与制造工艺深度融合。当前制造企业普遍存在以下痛点：

| 痛点类别 | 具体表现 |
|----------|----------|
| **设备运维** | 故障发现滞后、依赖人工巡检、维修经验难以传承 |
| **生产调度** | 排程依赖人工经验、插单响应慢、设备利用率不均衡 |
| **质量管控** | 质检数据分散、缺陷追溯困难、SPC 分析滞后 |
| **数据孤岛** | PLC/SCADA/MES/ERP 系统间数据不互通 |
| **知识管理** | 工艺文档散乱、老员工经验流失、新人培养周期长 |

### 5.2 候选选题方向

#### 选题一：设备故障诊断与预测性维护系统

**描述**：采集设备振动、温度、电流等传感器数据，基于机器学习模型实现故障诊断与剩余使用寿命（RUL）预测。

**技术栈**：

- 后端：Python + FastAPI
- 数据处理：Pandas / NumPy
- 模型：Scikit-learn / PyTorch（LSTM、CNN、Transformer）
- 前端：Streamlit / Vue.js
- 通信：MQTT / Modbus TCP 模拟数据采集

**核心功能**：

1. 设备数据实时采集与可视化
2. 故障类型自动分类（轴承故障、齿轮故障等）
3. RUL 预测与维护提醒
4. 历史数据查询与报表导出

**优势**：与专业方向（故障诊断、RUL 预测）高度契合，有公开数据集（CWRU、NASA Turbofan）可验证。

#### 选题二：车间生产调度与排程优化工具

**描述**：基于遗传算法/模拟退火等智能优化算法，实现车间作业调度（JSP / FJSP），支持甘特图可视化与插单重排。

**技术栈**：

- 后端：Python + FastAPI
- 算法：DEAP / 自研遗传算法
- 前端：Vue.js + ECharts 甘特图
- 数据库：SQLite / PostgreSQL

**核心功能**：

1. 工单与设备资源管理
2. 调度算法参数配置与运行
3. 甘特图可视化排程结果
4. 插单模拟与方案对比

**优势**：调度是智能制造经典问题，算法可解释性强，可视化效果好。

#### 选题三：制造质量数据分析与 SPC 平台

**描述**：对生产过程中的质量检测数据进行统计过程控制（SPC），实现控制图监控、过程能力指数（Cpk）计算、异常预警。

**技术栈**：

- 后端：Python + FastAPI
- 统计：SciPy / Statsmodels
- 前端：Streamlit / React
- 可视化：Plotly / ECharts

**核心功能**：

1. 质量数据批量导入
2. X-bar/R、P、C 等控制图生成
3. Cpk / PpK 过程能力分析
4. 异常模式识别（趋势、偏移、周期）

**优势**：SPC 是制造业质量管控基础工具，实用性强，数据需求低。

#### 选题四：AI 驱动的工艺知识问答助手

**描述**：基于 RAG（检索增强生成）技术，将企业工艺文档、设备手册、维修记录构建为知识库，支持自然语言问答。

**技术栈**：

- 框架：LangChain / LlamaIndex
- 向量库：Chroma / FAISS
- 模型：Qwen / DeepSeek（可本地部署）
- 前端：Gradio / Streamlit
- 文档解析：PyPDF / Unstructured

**核心功能**：

1. 工艺文档上传与向量化
2. 自然语言问答（带引用来源）
3. 多轮对话上下文管理
4. 知识库管理与更新

**优势**：直接体现 AI 编程 + 制造场景的结合，RAG 技术当前热度高。

### 5.3 选题推荐与理由

**综合推荐：选题一（设备故障诊断与预测性维护系统）**

理由：

1. **专业契合度最高**：与智能制造工程的故障诊断、RUL 预测研究方向直接对应，可复用已有知识积累。
2. **数据可获得性好**：CWRU 轴承数据集、NASA C-MAPSS 涡轮风扇数据集均为公开标准数据集，无需企业数据即可完成验证。
3. **技术栈完整**：涵盖数据采集、特征工程、模型训练、API 服务、前端可视化全链路，能充分展示 Vibe Coding 的全流程应用。
4. **展示效果好**：实时监控面板、故障分类结果、RUL 趋势曲线等可视化内容丰富，答辩演示效果好。
5. **可扩展性强**：后续可拓展数字孪生、边缘部署、多设备协同等方向。

### 5.4 项目架构设计（以选题一为例）

```
manufacturing-predictive-maintenance/
├── README.md
├── requirements.txt
├── .gitignore
├── config/
│   └── settings.yaml          # 配置文件
├── data/
│   ├── raw/                   # 原始数据
│   ├── processed/             # 处理后数据
│   └── sample/                # 示例数据
├── src/
│   ├── data_collection/       # 数据采集模块（MQTT/Modbus 模拟）
│   ├── preprocessing/         # 数据预处理与特征工程
│   ├── models/                # 模型定义与训练
│   ├── api/                   # FastAPI 后端服务
│   └── utils/                 # 工具函数
├── frontend/                  # 前端（Streamlit 或 Vue）
├── notebooks/                 # Jupyter 实验笔记
├── tests/                     # 单元测试
├── docs/                      # 项目文档
└── scripts/                   # 运行脚本
```

---

## 六、开发计划与里程碑

| 阶段 | 时间 | 任务 | 交付物 |
|------|------|------|--------|
| **第一阶段** | 第 1-2 周 | 环境搭建、工具学习、Git 工作流熟悉 | 仓库初始化、学习笔记 |
| **第二阶段** | 第 3-4 周 | 选题调研、数据集获取、技术方案确认 | 调研报告、技术方案文档 |
| **第三阶段** | 第 5-7 周 | 数据预处理、特征工程、模型训练与调优 | 训练好的模型、评估报告 |
| **第四阶段** | 第 8-9 周 | 后端 API 开发、前端可视化实现 | 可运行的 Web 应用 |
| **第五阶段** | 第 10 周 | 系统集成测试、Bug 修复、文档完善 | 完整应用、使用手册 |
| **第六阶段** | 第 11 周 | 答辩准备、演示视频录制 | 答辩 PPT、演示视频 |

---

## 七、学习资源推荐

### AI 编程

- Cursor 官方文档：https://docs.cursor.com
- Aider 官方文档：https://aider.chat
- 《Prompt Engineering Guide》：https://www.promptingguide.ai

### Git

- Pro Git（中文版）：https://git-scm.com/book/zh/v2
- Learn Git Branching（交互式练习）：https://learngitbranching.js.org

### 智能制造与故障诊断

- CWRU 轴承数据集：https://engineering.case.edu/bearingdatacenter
- NASA Prognostics Data Repository：https://www.nasa.gov/intelligent-systems-division
- Scikit-learn 官方文档：https://scikit-learn.org

---

## 八、总结

本次课程设计通过系统学习 Vibe Coding 方法与工具，旨在建立"AI 辅助 + 人工决策"的新型开发能力。核心收获包括：

1. **方法论层面**：掌握了意图驱动、迭代生成、上下文管理的 AI 编程范式，理解了 AI 的能力边界与人工审查的必要性。
2. **工具层面**：对比了 Cursor、Aider、Claude Code 等 Harness，确定了适合本项目的工具组合与模型选型策略。
3. **工程层面**：深入理解了 Git 的快照原理、对象模型、三区工作流与分支机制，建立了 AI 编程场景下的版本控制规范。
4. **领域层面**：通过对智能制造场景的需求调研，选定了设备故障诊断与预测性维护作为应用方向，确保项目既有技术深度又有工程价值。

后续将按照开发计划逐步推进，在实践中持续优化 AI 编程工作流，最终交付一个功能完善、可演示、可维护的制造场景应用程序。
