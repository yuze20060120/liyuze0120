# 方案设计

## 一、功能需求分析

### 1.1 系统总体定位

本系统面向旋转机械（以电机轴承为典型对象）的设备状态监测与预测性维护场景，提供从数据采集、信号处理、故障诊断到寿命预测的全链路功能。系统基于公开数据集进行开发与验证，采用模拟实时数据流的方式还原工业现场场景，最终以 Web 仪表盘形式交付可视化交互界面。

### 1.2 功能性需求

#### 1.2.1 数据采集与管理模块

| 需求编号 | 需求描述 | 优先级 |
|----------|----------|--------|
| FR-01 | 支持基于 CWRU 轴承数据集的信号回放，模拟实时传感器数据流 | 高 |
| FR-02 | 支持配置采样率（12 kHz / 48 kHz）、通道数、回放速度 | 高 |
| FR-03 | 支持手动上传 CSV / MAT 格式的振动信号文件进行离线分析 | 高 |
| FR-04 | 支持设备信息管理（设备编号、型号、安装位置、监测参数配置） | 中 |
| FR-05 | 支持历史数据的存储与查询，按时间范围检索 | 中 |

#### 1.2.2 信号处理与特征工程模块

| 需求编号 | 需求描述 | 优先级 |
|----------|----------|--------|
| FR-06 | 实现信号预处理：去趋势、巴特沃斯低通/带通滤波、归一化 | 高 |
| FR-07 | 提取时域特征：均值、标准差、峰值、峰峰值、峭度、偏度、裕度因子、波形因子、脉冲因子、峰值因子 | 高 |
| FR-08 | 提取频域特征：FFT 变换、重心频率、均方频率、频率方差、频谱熵 | 高 |
| FR-09 | 计算轴承特征频率（BPFO、BPFI、BSF、FTF）并提取对应频带能量 | 中 |
| FR-10 | 支持特征选择：方差阈值、相关性分析、基于模型的特征重要性排序 | 中 |
| FR-11 | 特征计算结果可导出为 CSV 格式 | 低 |

#### 1.2.3 故障诊断模块

| 需求编号 | 需求描述 | 优先级 |
|----------|----------|--------|
| FR-12 | 支持传统机器学习模型（随机森林、SVM、XGBoost）的训练与评估 | 高 |
| FR-13 | 支持深度学习模型（1D-CNN）的训练与评估，直接输入原始信号 | 高 |
| FR-14 | 支持故障类型分类：正常、内圈故障、外圈故障、滚动体故障 | 高 |
| FR-15 | 支持故障严重程度识别（不同故障直径） | 中 |
| FR-16 | 输出诊断结果：故障类型、置信度、特征贡献度分析 | 高 |
| FR-17 | 提供模型性能评估：准确率、精确率、召回率、F1、混淆矩阵 | 高 |
| FR-18 | 支持模型对比：在同一测试集上对比多种模型的性能 | 中 |

#### 1.2.4 RUL 预测模块

| 需求编号 | 需求描述 | 优先级 |
|----------|----------|--------|
| FR-19 | 基于 NASA C-MAPSS 数据集训练 RUL 预测模型（LSTM） | 中 |
| FR-20 | 输入传感器退化序列，输出剩余使用寿命预测值 | 中 |
| FR-21 | 展示退化趋势曲线与 RUL 预测区间 | 中 |
| FR-22 | 评估指标：RMSE、MAE、NASA 官方 Score 函数 | 中 |

#### 1.2.5 可视化仪表盘模块

| 需求编号 | 需求描述 | 优先级 |
|----------|----------|--------|
| FR-23 | 设备概览页：设备状态总览、健康度评分、告警数量统计 | 高 |
| FR-24 | 实时监控页：振动波形实时展示、频谱图、当前诊断结果 | 高 |
| FR-25 | 故障诊断页：上传信号 → 特征展示 → 诊断结果 → 置信度 | 高 |
| FR-26 | RUL 预测页：退化曲线、剩余寿命、维护建议时间窗口 | 中 |
| FR-27 | 历史分析页：趋势图、故障分布统计、模型性能对比 | 中 |
| FR-28 | 支持图表交互：缩放、悬停查看数值、导出图片 | 中 |

#### 1.2.6 告警与维护模块

| 需求编号 | 需求描述 | 优先级 |
|----------|----------|--------|
| FR-29 | 阈值告警：振动幅值、温度等指标超限时触发告警 | 中 |
| FR-30 | 异常检测告警：基于模型的异常状态识别 | 中 |
| FR-31 | 告警历史记录与查询 | 低 |
| FR-32 | 维护建议：根据 RUL 预测给出维护时间窗口建议 | 低 |

### 1.3 非功能性需求

| 类别 | 需求描述 |
|------|----------|
| **性能** | 单次故障诊断推理延迟 < 500ms（单条信号，长度 ≤ 4096 点） |
| **性能** | 仪表盘页面加载时间 < 3s，实时数据刷新频率 ≥ 1Hz |
| **可用性** | 提供一键启动脚本，依赖通过 requirements.txt 管理 |
| **可维护性** | 模块化设计，模块间低耦合，核心函数有类型注解和 docstring |
| **可扩展性** | 模型可插拔，新增诊断模型只需实现统一接口 |
| **兼容性** | 支持 Python 3.10+，兼容 Windows / Linux / macOS |
| **数据安全** | 配置文件和 API Key 通过环境变量管理，不硬编码 |

---

## 二、方案论证

### 2.1 选题可行性论证

#### 2.1.1 数据可行性

本项目采用两个公开标准数据集，无需依赖企业数据：

- **CWRU 轴承数据集**：凯斯西储大学公开的轴承故障诊断基准数据集，包含正常、内圈、外圈、滚动体四种状态，四种故障直径，四种负载条件，数据质量高，文献引用量极大，是故障诊断领域最常用的基准数据集。
- **NASA C-MAPSS 数据集**：NASA 公开的涡轮风扇发动机退化数据集，包含 4 个子数据集共 709 台发动机的全生命周期运行数据，是 RUL 预测领域的标准基准。

两个数据集均可免费下载，数据格式规范，有大量开源实现可参考，数据可行性高。

#### 2.1.2 技术可行性

系统各技术环节均有成熟的 Python 库支持：

| 技术环节 | 成熟方案 | 风险等级 |
|----------|----------|----------|
| 数据处理 | Pandas / NumPy | 低 |
| 信号处理 | SciPy.signal | 低 |
| 传统机器学习 | Scikit-learn / XGBoost | 低 |
| 深度学习 | PyTorch | 低 |
| 后端 API | FastAPI | 低 |
| 前端可视化 | Streamlit + Plotly | 低 |
| 数据存储 | SQLite | 低 |

所有技术均为社区广泛使用的成熟方案，文档丰富，遇到问题可快速检索解决方案，技术可行性高。

#### 2.1.3 开发周期可行性

项目按 11 周规划，各阶段任务明确，核心功能（数据采集、特征提取、故障诊断、基础仪表盘）可在 7 周内完成 MVP，剩余时间用于 RUL 预测、功能完善和文档撰写，周期可行性高。

### 2.2 技术方案对比论证

#### 2.2.1 前端方案对比

| 方案 | 开发效率 | 可视化能力 | 交互性 | 学习成本 | 推荐度 |
|------|----------|------------|--------|----------|--------|
| **Streamlit** | 极高（纯 Python） | 强（内置图表 + Plotly） | 中等 | 极低 | ★★★★★ |
| Flask + HTML/ECharts | 中等 | 强 | 高 | 中等 | ★★★☆☆ |
| Vue.js + ECharts | 低（需前后端分离） | 极强 | 极高 | 高 | ★★☆☆☆ |
| Gradio | 高 | 中等 | 中等 | 极低 | ★★★☆☆ |

**论证结论**：选择 Streamlit。课程设计周期有限，Streamlit 可使用纯 Python 快速构建数据仪表盘，内置组件丰富，与 Plotly 集成良好，能在短时间内交付高质量的可视化界面。Vue.js 方案虽然更专业，但开发量过大，不适合课程设计的时间约束。

#### 2.2.2 后端框架对比

| 框架 | 性能 | 自动文档 | 异步支持 | 类型安全 | 推荐度 |
|------|------|----------|----------|----------|--------|
| **FastAPI** | 极高（ASGI） | 内置 Swagger/ReDoc | 原生支持 | Pydantic 校验 | ★★★★★ |
| Flask | 中等 | 需插件 | 需扩展 | 无 | ★★★☆☆ |
| Django | 高 | 需插件 | 3.0+ 支持 | 有 | ★★☆☆☆ |

**论证结论**：选择 FastAPI。高性能、自动生成 API 文档、原生异步、Pydantic 数据校验，非常适合机器学习模型推理服务的开发。

#### 2.2.3 故障诊断模型方案对比

| 模型类型 | 代表模型 | 优点 | 缺点 | 定位 |
|----------|----------|------|------|------|
| 传统机器学习 | 随机森林、XGBoost | 可解释性强、训练快、无需 GPU | 依赖手工特征、上限有限 | 基线模型 |
| 深度学习 | 1D-CNN | 端到端学习、自动特征提取、精度高 | 可解释性弱、需较多数据 | 主模型 |
| 时序模型 | LSTM / GRU | 适合时序建模 | 训练慢、对信号分类不如 CNN | RUL 预测用 |

**论证结论**：采用"传统模型 + 深度学习"双轨方案。随机森林/XGBoost 作为基线，验证特征工程的有效性；1D-CNN 作为主模型，探索端到端故障诊断的性能上限。两者对比可丰富实验内容，提升报告深度。

### 2.3 Vibe Coding 适配性论证

本项目各模块边界清晰，非常适合拆解为小任务用 AI 逐步生成：

- **信号处理模块**：函数式编程，输入输出明确，AI 生成质量高
- **模型训练模块**：有大量公开参考代码，AI 可快速生成训练循环
- **API 开发模块**：FastAPI 有标准模式，AI 生成准确率高
- **前端仪表盘**：Streamlit 组件化，AI 可快速生成页面布局
- **测试与文档**：适合用 AI 批量生成单元测试和 docstring

同时，项目使用 Python 生态，AI 模型对 Python 代码的生成质量最高，Vibe Coding 适配性好。

---

## 三、方案规划

### 3.1 技术栈

#### 3.1.1 语言与运行环境

| 项目 | 选型 | 版本要求 | 说明 |
|------|------|----------|------|
| 编程语言 | Python | ≥ 3.10 | 全栈统一使用 Python |
| 包管理 | pip | - | 使用 requirements.txt 锁定依赖 |
| 虚拟环境 | venv / conda | - | 隔离项目依赖 |

#### 3.1.2 后端技术栈

| 类别 | 技术选型 | 版本 | 用途 |
|------|----------|------|------|
| Web 框架 | FastAPI | ≥ 0.110 | REST API 服务 |
| ASGI 服务器 | Uvicorn | ≥ 0.27 | 运行 FastAPI 应用 |
| 数据校验 | Pydantic | ≥ 2.0 | 请求/响应模型定义 |
| 数据库 | SQLite | 内置 | 设备信息、历史记录存储 |
| ORM | SQLAlchemy | ≥ 2.0 | 数据库操作 |
| 任务队列 | （可选）Celery + Redis | - | 异步模型训练任务 |
| 日志 | Loguru | ≥ 0.7 | 结构化日志 |

#### 3.1.3 数据处理与信号处理

| 类别 | 技术选型 | 版本 | 用途 |
|------|----------|------|------|
| 数值计算 | NumPy | ≥ 1.26 | 数组运算、FFT |
| 数据分析 | Pandas | ≥ 2.2 | 数据加载、表格处理 |
| 信号处理 | SciPy | ≥ 1.12 | 滤波、统计、频谱分析 |
| 小波变换 | PyWavelets | ≥ 1.5 | 时频分析（可选） |
| MAT 文件读取 | SciPy.io | - | 读取 .mat 格式数据集 |

#### 3.1.4 机器学习与深度学习

| 类别 | 技术选型 | 版本 | 用途 |
|------|----------|------|------|
| 传统机器学习 | Scikit-learn | ≥ 1.4 | RF、SVM、评估指标、特征选择 |
| 梯度提升 | XGBoost | ≥ 2.0 | 对比模型 |
| 深度学习 | PyTorch | ≥ 2.2 | 1D-CNN、LSTM 模型 |
| 模型可视化 | TorchInfo | - | 模型结构打印 |

#### 3.1.5 前端可视化

| 类别 | 技术选型 | 版本 | 用途 |
|------|----------|------|------|
| 仪表盘框架 | Streamlit | ≥ 1.30 | Web 界面 |
| 交互式图表 | Plotly | ≥ 5.19 | 波形图、频谱图、趋势图 |
| 静态图表 | Matplotlib | ≥ 3.8 | 混淆矩阵、训练曲线 |
| 数据展示 | Streamlit AgGrid | - | 交互式表格（可选） |

#### 3.1.6 开发与质量工具

| 类别 | 技术选型 | 用途 |
|------|----------|------|
| 代码格式化 | Ruff | Lint + 格式化 |
| 类型检查 | Mypy | 静态类型检查 |
| 单元测试 | Pytest | 测试框架 |
| 覆盖率 | pytest-cov | 测试覆盖率统计 |
| AI 编程 Harness | Cursor + Aider | AI 辅助开发 |
| 版本控制 | Git | 代码版本管理 |

### 3.2 需配置的外部资源

#### 3.2.1 AI 编程工具资源

| 资源 | 用途 | 获取方式 | 费用 |
|------|------|----------|------|
| Cursor Pro | 主力 AI 编程编辑器 | cursor.com 注册订阅 | $20/月（学生可申请优惠） |
| Aider | 终端 AI 编程工具 | pip install aider-chat | 免费（需 API Key） |
| DeepSeek API | Aider 后端模型 | platform.deepseek.com | 按量付费（极低） |
| Anthropic API | Claude 模型调用 | console.anthropic.com | 按量付费 |
| ChatGPT Plus | 方案设计与算法推导 | chat.openai.com | $20/月 |

> 说明：如预算有限，可使用 Cursor 免费版（有使用量限制）+ DeepSeek API（成本极低）+ 国产免费 AI 编程工具（通义灵码、豆包 MarsCode）完成开发。

#### 3.2.2 开发环境资源

| 资源 | 配置要求 | 说明 |
|------|----------|------|
| 计算机 | 8GB+ 内存，推荐 16GB | 深度学习训练建议有独立显卡（NVIDIA，4GB+ 显存） |
| 操作系统 | Windows 10/11、Ubuntu 20.04+、macOS 12+ | 跨平台兼容 |
| Python | 3.10 ~ 3.12 | 推荐 3.12 |
| Git | ≥ 2.30 | 版本控制 |
| GitHub 账号 | - | 代码仓库托管 |

#### 3.2.3 数据集资源

详见 3.3 节。

### 3.3 数据集来源

#### 3.3.1 CWRU 轴承故障诊断数据集

| 项目 | 说明 |
|------|------|
| **提供方** | 凯斯西储大学（Case Western Reserve University）轴承数据中心 |
| **设备** | 2 马力 Reliance 电机，驱动端和风扇端各安装加速度传感器 |
| **采样频率** | 12 kHz 和 48 kHz 两种 |
| **故障类型** | 正常（Normal）、内圈故障（Inner Race）、外圈故障（Outer Race）、滚动体故障（Ball） |
| **故障直径** | 0.007、0.014、0.021、0.028 英寸 |
| **负载条件** | 0、1、2、3 马力（对应电机转速约 1797~1720 RPM） |
| **数据格式** | MATLAB .mat 文件 |
| **下载地址** | https://engineering.case.edu/bearingdatacenter |
| **用途** | 故障诊断模型的训练、验证与测试 |

**数据划分方案**：
- 训练集：70%（随机选取不同工况下的样本）
- 验证集：15%
- 测试集：15%
- 采用分层抽样，保证各故障类型比例一致

#### 3.3.2 NASA C-MAPSS 涡轮风扇发动机退化数据集

| 项目 | 说明 |
|------|------|
| **提供方** | NASA Prognostics Center of Excellence |
| **设备** | C-MAPSS 商用涡轮风扇发动机仿真模型 |
| **子数据集** | FD001、FD002、FD003、FD004 共 4 个 |
| **数据规模** | 共 709 台发动机的运行至故障数据 |
| **传感器** | 21 个传感器通道（温度、压力、转速等） |
| **操作条件** | 6 种操作条件（FD001 仅 1 种，适合入门） |
| **数据格式** | 文本文件（空格分隔） |
| **下载地址** | https://www.nasa.gov/intelligent-systems-division |
| **用途** | RUL 预测模型的训练与评估 |

**入门选择**：优先使用 FD001 子数据集（单一操作条件、单一故障模式），降低建模难度。

#### 3.3.3 辅助数据集（可选）

| 数据集 | 来源 | 用途 |
|--------|------|------|
| NASA IMS 轴承数据集 | NASA | 轴承全生命周期退化数据，可用于 RUL 验证 |
| PU 轴承数据集 | 帕德博恩大学 | 更丰富工况下的故障诊断验证 |
| MFPT 数据集 | 机械故障预防技术学会 | 小规模数据集，用于快速验证 |

### 3.4 前端 UI 方案

#### 3.4.1 页面结构

系统采用 Streamlit 多页面架构，共 5 个页面：

```
主入口（dashboard.py）
├── 1_设备概览.py      # 首页：状态总览、健康度、告警统计
├── 2_实时监控.py      # 振动波形、频谱图、实时诊断
├── 3_故障诊断.py      # 上传信号、特征提取、诊断结果
├── 4_RUL预测.py       # 退化曲线、寿命预测、维护建议
└── 5_历史分析.py      # 趋势图、故障分布、模型对比
```

#### 3.4.2 页面设计规范

- **布局**：左侧边栏导航 + 主内容区，使用 Streamlit 原生布局
- **配色**：采用工业风格配色，主色深蓝色（#1E3A5F），辅助色橙色（#E87722）告警色红色（#DC3545），正常色绿色（#28A745）
- **图表**：统一使用 Plotly，支持交互缩放、悬停提示、导出 PNG
- **响应式**：适配桌面端浏览器，最小宽度 1280px
- **字体**：默认无衬线字体，标题加粗，数据使用等宽字体

#### 3.4.3 核心页面 wireframe

**设备概览页**：
```
┌─────────────────────────────────────────────────────┐
│  设备概览                              [刷新] [设置] │
├──────────┬──────────┬──────────┬────────────────────┤
│ 设备总数  │ 正常运行  │ 告警中    │ 维护中             │
│   12     │   10     │   1     │   1                │
├──────────┴──────────┴──────────┴────────────────────┤
│  设备健康度分布（饼图）    │  近 7 天告警趋势（折线图） │
│                           │                          │
├───────────────────────────┴──────────────────────────┤
│  设备列表（表格：设备编号 / 状态 / 健康度 / 最后诊断）  │
└──────────────────────────────────────────────────────┘
```

**实时监控页**：
```
┌─────────────────────────────────────────────────────┐
│  实时监控              [设备选择▼] [启动/停止] [速度▼] │
├─────────────────────────────────────────────────────┤
│  振动信号时域波形（Plotly 折线图，实时滚动）            │
│  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ │
├──────────────────────────┬──────────────────────────┤
│  频谱图（Plotly 柱状图）   │  当前诊断结果              │
│                          │  状态：⚠️ 内圈故障          │
│                          │  置信度：92.3%             │
│                          │  建议：尽快安排检查         │
└──────────────────────────┴──────────────────────────┘
```

**故障诊断页**：
```
┌─────────────────────────────────────────────────────┐
│  故障诊断                                             │
├──────────────────────┬──────────────────────────────┤
│  数据输入区            │  诊断结果区                   │
│  [上传文件]            │  ┌──────────────────────┐   │
│  或选择示例数据 ▼      │  │ 故障类型：内圈故障     │   │
│  [开始诊断] 按钮       │  │ 置信度：94.5%         │   │
│                      │  │ 严重程度：中等         │   │
│                      │  └──────────────────────┘   │
│                      │  特征贡献度（横向条形图）       │
├──────────────────────┴──────────────────────────────┤
│  时域特征表  │  频域特征表  │  波形图  │  频谱图        │
└─────────────────────────────────────────────────────┘
```

### 3.5 后端算法方案

#### 3.5.1 信号处理算法

**预处理流程**：
```
原始信号 → 去趋势（减去线性拟合） → 巴特沃斯带通滤波（500~10000 Hz）
         → 归一化（Z-score 或 Min-Max） → 输出干净信号
```

**时域特征计算公式**：

| 特征 | 公式 | 物理意义 |
|------|------|----------|
| 均值 μ | Σxᵢ / N | 信号直流分量 |
| 标准差 σ | √(Σ(xᵢ-μ)² / N) | 信号波动程度 |
| 峰值 | max(\|xᵢ\|) | 最大冲击幅值 |
| 峰峰值 | max(xᵢ) - min(xᵢ) | 信号动态范围 |
| 峭度 | Σ(xᵢ-μ)⁴ / (N·σ⁴) | 冲击特性（正常≈3，故障>3） |
| 偏度 | Σ(xᵢ-μ)³ / (N·σ³) | 信号对称性 |
| 裕度因子 | max(\|xᵢ\|) / (Σ√\|xᵢ\| / N)² | 冲击检测 |
| 波形因子 | RMS / (Σ\|xᵢ\| / N) | 波形形状 |
| 脉冲因子 | max(\|xᵢ\|) / (Σ\|xᵢ\| / N) | 脉冲检测 |
| 峰值因子 | max(\|xᵢ\|) / RMS | 峰值检测 |

**频域特征**：
- FFT：使用 `scipy.fft.fft`，去除直流分量后取单边谱
- 重心频率：Σ(fᵢ · Pᵢ) / ΣPᵢ
- 均方频率：√(Σ(fᵢ² · Pᵢ) / ΣPᵢ)
- 频率方差：Σ((fᵢ - fc)² · Pᵢ) / ΣPᵢ
- 频谱熵：-Σ(Pᵢ · log(Pᵢ)) / log(N)

**轴承特征频率**（以深沟球轴承为例）：
- BPFO（外圈故障频率）= (Z/2) · fr · (1 - d·cosα / D)
- BPFI（内圈故障频率）= (Z/2) · fr · (1 + d·cosα / D)
- BSF（滚动体故障频率）= (D/(2d)) · fr · (1 - (d·cosα/D)²)
- FTF（保持架故障频率）= (1/2) · fr · (1 - d·cosα / D)

其中 Z 为滚动体数，fr 为轴转速，d 为滚动体直径，D 为节径，α 为接触角。

#### 3.5.2 故障诊断算法

**模型一：随机森林（基线）**
- 输入：手工提取的时域 + 频域特征（约 20 维）
- 算法：Scikit-learn RandomForestClassifier
- 超参数：n_estimators=200, max_depth=15, min_samples_split=5
- 优势：训练快、可解释性强（feature_importances_）、不易过拟合
- 预期准确率：≥ 90%

**模型二：XGBoost（对比）**
- 输入：同随机森林
- 算法：XGBClassifier
- 超参数：n_estimators=300, max_depth=6, learning_rate=0.1
- 优势：精度通常优于随机森林
- 预期准确率：≥ 92%

**模型三：1D-CNN（主模型）**
- 输入：原始振动信号（长度 1024 或 4096，归一化后）
- 网络结构：
  ```
  Input (1, L)
  → Conv1d(1→32, kernel=64, stride=8) → BN → ReLU → MaxPool(2)
  → Conv1d(32→64, kernel=16, stride=2) → BN → ReLU → MaxPool(2)
  → Conv1d(64→128, kernel=8, stride=1) → BN → ReLU → MaxPool(2)
  → Flatten → Dropout(0.5)
  → Linear(128→64) → ReLU → Dropout(0.3)
  → Linear(64→num_classes) → Softmax
  ```
- 训练：Adam 优化器，学习率 1e-3，交叉熵损失，Batch Size 64，Epoch 50
- 数据增强：添加高斯噪声、随机缩放、时间平移
- 预期准确率：≥ 95%

**模型评估**：
- 指标：准确率（Accuracy）、精确率（Precision）、召回率（Recall）、F1 分数
- 可视化：混淆矩阵（Matplotlib）、分类报告、ROC 曲线（二分类扩展）
- 可解释性：SHAP 值分析（传统模型）、Grad-CAM（CNN 关注区域）

#### 3.5.3 RUL 预测算法

**模型：LSTM 回归网络**
- 输入：滑动窗口截取的传感器序列（窗口大小 30~50 个时间步，多通道）
- 网络结构：
  ```
  Input (seq_len, num_sensors)
  → LSTM(hidden=64, num_layers=2, dropout=0.2)
  → 取最后时间步输出
  → Linear(64→32) → ReLU → Dropout(0.3)
  → Linear(32→1) → ReLU（RUL ≥ 0）
  ```
- 标签处理：RUL 分段线性（早期恒定最大值，后期线性下降）
- 训练：Adam 优化器，MSE 损失，Batch Size 128，Epoch 100
- 评估：RMSE、MAE、NASA Score 函数（Score = Σexp(-(RUL_actual - RUL_pred)/13) for underestimation, Σexp((RUL_actual - RUL_pred)/10) for overestimation）

#### 3.5.4 算法模块接口设计

统一模型接口，便于扩展和对比：

```python
class BaseDiagnosisModel(ABC):
    """故障诊断模型基类"""

    @abstractmethod
    def train(self, X_train, y_train, X_val, y_val) -> dict:
        """训练模型，返回训练历史"""

    @abstractmethod
    def predict(self, X) -> np.ndarray:
        """预测故障类型"""

    @abstractmethod
    def predict_proba(self, X) -> np.ndarray:
        """预测各类别概率"""

    @abstractmethod
    def evaluate(self, X_test, y_test) -> dict:
        """评估模型，返回指标字典"""

    @abstractmethod
    def save(self, path: str) -> None:
        """保存模型"""

    @abstractmethod
    def load(self, path: str) -> None:
        """加载模型"""
```

### 3.6 系统架构

```
┌──────────────────────────────────────────────────────────────┐
│                        前端展示层                              │
│              Streamlit Dashboard（5 个页面）                   │
│         设备概览 / 实时监控 / 故障诊断 / RUL预测 / 历史分析      │
└──────────────────────────┬───────────────────────────────────┘
                           │ HTTP REST / WebSocket
┌──────────────────────────┴───────────────────────────────────┐
│                        后端服务层                              │
│                    FastAPI Application                         │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌────────────────┐  │
│  │ 数据接口  │ │ 诊断接口  │ │ 预测接口  │ │ 设备/告警管理   │  │
│  │ /data/*  │ │ /diag/*  │ │ /rul/*   │ │ /admin/*       │  │
│  └──────────┘ └──────────┘ └──────────┘ └────────────────┘  │
└────┬──────────────┬──────────────┬──────────────┬────────────┘
     │              │              │              │
┌────┴─────┐ ┌──────┴─────┐ ┌────┴──────┐ ┌─────┴──────┐
│ 数据采集  │ │ 特征工程    │ │ 模型推理   │ │ 告警引擎    │
│ 模拟器   │ │ 时域/频域   │ │ 诊断+RUL  │ │ 阈值/异常   │
└────┬─────┘ └──────┬─────┘ └────┬──────┘ └─────┬──────┘
     │              │              │              │
┌────┴──────────────┴──────────────┴──────────────┴────────────┐
│                        数据存储层                              │
│   SQLite（设备/告警/历史记录） + 文件系统（模型/数据集/CSV）    │
└──────────────────────────────────────────────────────────────┘
```

---

## 四、技术路线

### 4.1 总体技术路线

本项目采用"自底向上、逐层验证"的技术路线，按照数据层 → 算法层 → 服务层 → 展示层的顺序逐步构建，每一层完成后进行验证再进入下一层，确保系统稳定性。

```
阶段1：环境搭建与数据准备
  ├─ 配置开发环境（Python、Git、Cursor、Aider）
  ├─ 下载并探索 CWRU / C-MAPSS 数据集
  ├─ 编写数据加载器，完成数据清洗与划分
  └─ 验证：数据加载正确，样本分布均衡

阶段2：信号处理与特征工程
  ├─ 实现预处理（去趋势、滤波、归一化）
  ├─ 实现时域特征提取（10+ 维）
  ├─ 实现频域特征提取（FFT + 统计特征）
  ├─ 实现轴承特征频率计算
  └─ 验证：特征计算正确，与文献值对比

阶段3：故障诊断模型开发
  ├─ 训练随机森林基线模型
  ├─ 训练 XGBoost 对比模型
  ├─ 构建并训练 1D-CNN 主模型
  ├─ 模型评估与对比分析
  └─ 验证：准确率达到预期，混淆矩阵合理

阶段4：RUL 预测模型开发
  ├─ 处理 C-MAPSS 数据，构建滑动窗口数据集
  ├─ 构建 LSTM 回归模型
  ├─ 训练与调优
  ├─ 评估（RMSE、MAE、Score）
  └─ 验证：预测趋势合理，指标达标

阶段5：后端 API 开发
  ├─ 搭建 FastAPI 项目骨架
  ├─ 实现诊断推理接口
  ├─ 实现 RUL 预测接口
  ├─ 实现设备/告警管理接口
  ├─ 实现 WebSocket 实时数据推送
  └─ 验证：API 文档可访问，接口测试通过

阶段6：前端仪表盘开发
  ├─ 搭建 Streamlit 多页面框架
  ├─ 开发设备概览页
  ├─ 开发实时监控页
  ├─ 开发故障诊断页
  ├─ 开发 RUL 预测页
  ├─ 开发历史分析页
  └─ 验证：页面可正常访问，交互流畅

阶段7：系统集成与优化
  ├─ 前后端联调
  ├─ 性能优化（推理延迟、页面加载）
  ├─ 错误处理与边界情况测试
  ├─ 编写单元测试
  └─ 验证：全流程可运行，无关键 Bug

阶段8：文档撰写与答辩准备
  ├─ 完善项目文档（README、API 文档、使用手册）
  ├─ 撰写课程设计报告
  ├─ 录制演示视频
  ├─ 准备答辩 PPT
  └─ 验证：文档完整，演示流畅
```

### 4.2 Vibe Coding 技术路线

在开发过程中，AI 编程工具按以下方式介入各阶段：

| 开发阶段 | AI 工具 | 使用方式 |
|----------|---------|----------|
| 环境搭建 | Cursor Chat | 询问依赖安装、配置问题 |
| 数据探索 | Cursor + ChatGPT | 生成数据加载代码、可视化脚本 |
| 信号处理 | Cursor Inline Edit | 生成特征提取函数，逐步验证修正 |
| 模型开发 | Cursor Composer | 同时生成模型定义、训练脚本、评估代码 |
| API 开发 | Cursor Composer | 生成路由、Pydantic 模型、错误处理 |
| 前端开发 | Cursor Chat | 生成 Streamlit 页面布局和图表代码 |
| 测试编写 | Aider + DeepSeek | 批量生成单元测试 |
| 代码重构 | Aider | 描述重构需求，自动修改并 commit |
| 文档撰写 | ChatGPT / Claude | 生成 README、API 文档、注释 |

---

## 五、计划安排

### 5.1 总体时间规划

项目总周期 11 周，按以下阶段分配：

| 阶段 | 周次 | 主要任务 | 里程碑交付物 |
|------|------|----------|-------------|
| **第一阶段** | 第 1 周 | 环境搭建、工具学习、数据集获取与探索 | 环境就绪、数据加载器、选题说明 |
| **第二阶段** | 第 2-3 周 | 信号处理与特征工程模块开发 | 特征提取模块、特征分析报告 |
| **第三阶段** | 第 4-5 周 | 故障诊断模型开发与评估 | 3 种诊断模型、评估报告 |
| **第四阶段** | 第 6 周 | RUL 预测模型开发 | LSTM 模型、RUL 评估结果 |
| **第五阶段** | 第 7 周 | 后端 API 开发 | FastAPI 服务、接口文档 |
| **第六阶段** | 第 8 周 | 前端仪表盘开发 | Streamlit 5 页面 |
| **第七阶段** | 第 9 周 | 系统集成、测试、优化 | 可运行完整系统、测试报告 |
| **第八阶段** | 第 10-11 周 | 文档撰写、演示视频、答辩准备 | 完整文档、演示视频、答辩 PPT |

### 5.2 详细周计划

**第 1 周：环境搭建与数据准备**
- 周一-周二：配置 Python 环境、安装 Git、注册并配置 Cursor / Aider
- 周三：下载 CWRU 和 C-MAPSS 数据集，阅读数据说明文档
- 周四：编写数据加载器（.mat 文件读取、CSV 读取）
- 周五：数据探索性分析（样本分布、信号可视化），完成选题说明
- 周末：提交选题说明到仓库，整理学习笔记

**第 2 周：信号预处理**
- 实现去趋势、巴特沃斯滤波函数
- 实现归一化方法（Z-score、Min-Max）
- 编写预处理模块单元测试
- 验证滤波效果（对比滤波前后频谱）

**第 3 周：特征工程**
- 实现 10+ 维时域特征
- 实现 FFT 与频域统计特征
- 实现轴承特征频率计算
- 特征可视化与相关性分析
- 完成特征工程模块，导出特征数据集

**第 4 周：传统机器学习模型**
- 训练随机森林基线模型
- 训练 XGBoost 对比模型
- 超参数调优（GridSearchCV）
- 模型评估与对比
- 特征重要性分析

**第 5 周：深度学习模型**
- 构建 1D-CNN 网络结构
- 数据增强（噪声、缩放、平移）
- 模型训练与调优
- 模型评估（混淆矩阵、分类报告）
- 与传统模型对比分析

**第 6 周：RUL 预测**
- C-MAPSS 数据预处理与滑动窗口构建
- LSTM 模型搭建
- 模型训练与调优
- 评估（RMSE、MAE、NASA Score）
- 退化曲线可视化

**第 7 周：后端 API**
- FastAPI 项目骨架搭建
- 诊断推理接口（/api/diagnose）
- RUL 预测接口（/api/predict-rul）
- 设备管理与告警接口
- WebSocket 实时数据推送
- 接口测试（Swagger UI + pytest）

**第 8 周：前端仪表盘**
- Streamlit 多页面框架搭建
- 设备概览页开发
- 实时监控页开发
- 故障诊断页开发
- RUL 预测页开发
- 历史分析页开发

**第 9 周：集成与优化**
- 前后端联调
- 性能测试与优化
- 边界情况与错误处理
- 单元测试补充（覆盖率 ≥ 70%）
- Bug 修复

**第 10 周：文档与完善**
- 撰写 README.md（项目说明、安装、使用）
- 撰写 API 文档
- 撰写使用手册
- 完善代码注释和 docstring
- 代码审查与重构

**第 11 周：答辩准备**
- 录制系统演示视频（5-10 分钟）
- 制作答辩 PPT
- 模拟答辩
- 最终代码提交与仓库整理

### 5.3 关键节点与风险应对

| 关键节点 | 时间 | 风险 | 应对措施 |
|----------|------|------|----------|
| 特征工程完成 | 第 3 周末 | 特征计算错误导致模型效果差 | 与文献对比验证，编写单元测试 |
| CNN 模型训练完成 | 第 5 周末 | 准确率不达标 | 调整网络结构、增加数据增强、检查标签 |
| 前后端联调完成 | 第 9 周中 | 接口不兼容 | 提前定义 API 规范，使用 Pydantic 校验 |
| 系统可运行 | 第 9 周末 | 存在阻塞性 Bug | 提前预留 3 天缓冲期 |
| 文档完成 | 第 10 周末 | 时间不足 | 边开发边记录，每周更新文档 |

---

## 六、参考文献

### 6.1 故障诊断与预测性维护

[1] Lei Y, Yang B, Jiang X, et al. Applications of machine learning to machine fault diagnosis: A review and roadmap[J]. Mechanical Systems and Signal Processing, 2020, 138: 106587.

[2] Jardine A K S, Lin D, Banjevic D. A review on machinery diagnostics and prognostics implementing condition-based maintenance[J]. Mechanical Systems and Signal Processing, 2006, 20(7): 1483-1510.

[3] 雷亚国, 贾峰, 周昕, 等. 基于深度学习理论的机械装备大数据健康监测方法[J]. 机械工程学报, 2015, 51(21): 49-56.

[4] Smith W A, Randall R B. Rolling element bearing diagnostics using the Case Western Reserve University data: A benchmark study[J]. Mechanical Systems and Signal Processing, 2015, 64-65: 100-131.

### 6.2 深度学习故障诊断

[5] Zhang W, Peng G, Li C, et al. A new deep learning model for fault diagnosis with good anti-noise and domain adaptation ability on raw vibration signals[J]. Sensors, 2017, 17(2): 425.

[6] Eren L, Ince T, Kiranyaz S. A generic intelligent bearing fault diagnosis system using compact adaptive 1D CNN classifier[J]. Journal of Signal Processing Systems, 2019, 91(2): 179-189.

[7] 赵光权, 彭喜元, 刘大同. 数据驱动的预测与健康管理综述[J]. 仪器仪表学报, 2018, 39(8): 1-12.

### 6.3 RUL 预测

[8] Sateesh Babu G, Zhao P, Li X L. Deep convolutional neural network based regression approach for estimation of remaining useful life[C]//International conference on database systems for advanced applications. Springer, 2016: 214-228.

[9] Zheng S, Ristovski K, Farahat A, et al. Long short-term memory network for remaining useful life estimation[C]//2017 IEEE international conference on prognostics and health management (ICPHM). IEEE, 2017: 88-95.

[10] Saxena A, Goebel K, Simon D, et al. Damage propagation modeling for aircraft engine run-to-failure simulation[C]//2008 International conference on prognostics and health management. IEEE, 2008: 1-9.

### 6.4 Vibe Coding 与 AI 编程

[11] Vaithilingam P, Zhang T, Glassman E L. Grounded copilot: How programmers interact with code-generating models[C]//Proceedings of the 2023 CHI Conference on Human Factors in Computing Systems. 2023: 1-23.

[12] Peng B, Li C, He P, et al. CodeGen2: Lessons for training on programming and natural languages[J]. arXiv preprint arXiv:2305.02309, 2023.

[13] 李昂, 王健. AI 辅助编程工具的发展现状与趋势分析[J]. 计算机工程与应用, 2024, 60(5): 1-12.

### 6.5 数据集与工具文档

[14] Case Western Reserve University. Bearing Data Center[EB/OL]. https://engineering.case.edu/bearingdatacenter, 2024.

[15] NASA Prognostics Center of Excellence. Data Repository[EB/OL]. https://www.nasa.gov/intelligent-systems-division, 2024.

[16] FastAPI Documentation[EB/OL]. https://fastapi.tiangolo.com, 2024.

[17] Streamlit Documentation[EB/OL]. https://docs.streamlit.io, 2024.

[18] PyTorch Documentation[EB/OL]. https://pytorch.org/docs, 2024.

[19] Scikit-learn Documentation[EB/OL]. https://scikit-learn.org/stable, 2024.

[20] Aider: AI pair programming in your terminal[EB/OL]. https://aider.chat, 2024.
